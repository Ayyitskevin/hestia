name: ci-smoke

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"

      - name: Install
        run: |
          pip install --require-hashes -r requirements/dev.lock
          pip install --no-deps --no-build-isolation -e .

      - name: Verify dependency locks
        run: bash scripts/lock-dependencies.sh --check

      - name: Enforce aggregate revenue-spine coverage floor
        run: bash scripts/coverage.sh

      - name: Build and boot installed wheel
        run: bash scripts/wheel-smoke.sh

      - name: Build and boot production container
        run: bash scripts/container-smoke.sh

      - name: Lint + tests + runtime invariants + dogfood
        run: bash scripts/ci-smoke.sh

      - name: Scratch backup and restore drill
        run: bash scripts/restore-drill.sh

      # Advisory supply-chain scan: surfaces known-CVE dependencies in the CI log without
      # blocking the merge on an unfixable transitive/tooling CVE. A solo operator reviews
      # the output and bumps constraints (see docs/security.md). --skip-editable ignores
      # the hestia package itself.
      - name: Dependency vulnerability scan (advisory)
        continue-on-error: true
        run: pip-audit --skip-editable --progress-spinner=off
